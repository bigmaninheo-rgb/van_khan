[project]
name = "van-khan"
version = "1.0.0"
description = "Van Khan - Ung dung Van Khan Cung Bai"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
app = "main:main"
